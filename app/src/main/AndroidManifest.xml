<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.ec.todolist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SettingsActivity"
            android:label="@string/SettingsActivityLabel" />
        <activity
            android:name=".AproposActivity"
            android:label="@string/AproposActivityLabel" />
        <activity
            android:name=".ShowListActivity"
            android:label="@string/ShowListActivityLabel" />
        <activity android:name=".BasicActivity" />
        <activity
            android:name=".ChoixListActivity"
            android:label="@string/ChoixListeActivityLabel" />
        <activity
            android:name=".MainActivity"
            android:label="@string/MainActivityLabel">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>